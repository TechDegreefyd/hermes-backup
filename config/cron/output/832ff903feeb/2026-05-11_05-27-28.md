# Cron Job: Daily Regular API Recon (FAILED)

**Job ID:** 832ff903feeb
**Run Time:** 2026-05-11 05:27:28
**Schedule:** 0 9 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Generate and deliver the Regular API Recon Report for YESTERDAY's date. TWO versions: ALL Sources and Branded Campaigns.

## ALL Sources Query
```sql
-- Recon data
SELECT r.college_name, r.sent_type, r.api_sent_status, COUNT(DISTINCT r.student_id)
FROM student_college_api_sent_status r
WHERE DATE(r.created_at AT TIME ZONE 'Asia/Kolkata') = 'YYYY-MM-DD'
GROUP BY 1,2,3;

-- LMS per college
SELECT r.college_name, COUNT(DISTINCT sla.student_id)
FROM student_college_api_sent_status r
JOIN student_lead_activities sla ON sla.student_id = r.student_id
WHERE DATE(r.created_at AT TIME ZONE 'Asia/Kolkata') = 'YYYY-MM-DD'
GROUP BY 1;
```

## Branded Campaigns Query
Filter by campaign names/IDs via latest utm_campaign:
```sql
WITH latest_utm AS (
    SELECT DISTINCT ON (student_id) student_id, utm_campaign
    FROM student_lead_activities ORDER BY student_id, created_at DESC
)
SELECT r.college_name, r.sent_type, r.api_sent_status, COUNT(DISTINCT r.student_id)
FROM student_college_api_sent_status r
JOIN latest_utm lu ON lu.student_id = r.student_id
WHERE DATE(r.created_at AT TIME ZONE 'Asia/Kolkata') = 'YYYY-MM-DD'
  AND (lu.utm_campaign IN ('Amity_Online_Admission_MBA','Amity_University_Gurugram','Amity_University_Raipur','Amity_University_Lucknow','Amity_University_Mumbai','Amity_University_Gwalior','Partner_Amity_Online_2026','Amity_Online_V1','LPU_Online_V1','VGU_Online_V1','Manipal_Online_V1','CU_Online_V1','Shoolini_Online_V1','Shoolini_Online_V2','GLA_Online_V1','GLA_Online_V2','CU_Online_Admission','CU_Online_Admission_Delta','CU_Online_Admission_Zeta','Shoolini_Online_Delta','LPU_Online_Alpha','LPU_Online_Gamma','LPU_Online_Zeta')
    OR lu.utm_campaign IN ('23659350616','23807086200','23810994645','23814823859','23820721369','23821027168','23228113322','23794794232','23794010280','23772025619','23779002914','23794940566','23767340817','23798269338','23772157658','23803352159','23470383548','23502437890','23676777747','23534722448','23486436393','23486463996','23675435222'))
GROUP BY 1,2,3;

-- LMS per college (same filter)
WITH latest_utm AS (...)
SELECT r.college_name, COUNT(DISTINCT sla.student_id)
FROM student_college_api_sent_status r
JOIN latest_utm lu ON lu.student_id = r.student_id
JOIN student_lead_activities sla ON sla.student_id = r.student_id
WHERE DATE(r.created_at AT TIME ZONE 'Asia/Kolkata') = 'YYYY-MM-DD'
  AND (same campaign filter)
GROUP BY 1;
```

## HTML Template
Use Barlow font dark theme (bg #0b1623). Table: College Name | Auto Recon (Proc/Fail/DNP) | Manual Recon (Proc/Fail/DNP) | Total (Proc/Fail/DNP) | Total Leads. Green leads column (#3ddc84). Grand Total + Summary Total rows.

## Status Mapping
- Proceed: api_sent_status = 'Proceed'
- Failed: api_sent_status contains 'Technical Issues' or 'Failed'  
- DNP: everything else

## Delivery
- Save: /home/mohit/workspace/Regular_Recon_All_YYYY-MM-DD.html and Regular_Recon_Branded_YYYY-MM-DD.html
- Send BOTH to Telegram DM (Mohit Kapoor) with summary stats
- NEVER WhatsApp

## DB
regular_lms from /home/mohit/workspace/.env

## Error

```
RuntimeError: HTTP 404: Gemini HTTP 404 (NOT_FOUND): models/deepseek-v4-pro is not found for API version v1beta, or is not supported for generateContent. Call ListModels to see the list of available models and their supported methods.
```
