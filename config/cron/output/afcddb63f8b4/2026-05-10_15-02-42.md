# Cron Job: Daily Regular LMS Reports

**Job ID:** afcddb63f8b4
**Run Time:** 2026-05-10 15:02:42
**Schedule:** 0 15 * * *

## Prompt

[IMPORTANT: The user has invoked the "regular-lms-reporting" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: regular-lms-reporting
description: Procedure for generating the Daily Regular LMS reports, which involves federating data from three separate databases (Regular, CGC, and Amity) and applying specific entity mappings.
trigger: "When asked to generate, update, or troubleshoot reports for the 'Regular' LMS track."
---

# Regular LMS Reporting Workflow

The Regular LMS reporting system differs from the Online track by pulling from three distinct PostgreSQL databases and merging them into a single analytical view.

## Model Requirements & Fallback

⚠️ **CRITICAL**: This skill requires a model capable of:
- Multi-step orchestration (config parsing → multi-database federation → data transformation → file generation → delivery)
- Complex async database queries across 3–4 distinct PostgreSQL instances
- Hierarchical data mapping (supervisor trees, campus consolidation, counsellor resolution)

**Minimum Model Tier**: Claude Sonnet 3.5+ or Gemini 3.1 Pro.
**DO NOT use**: Haiku, Flash Lite, or any "fast" tier — they will fail silently, produce incomplete output, or crash on database joins.

**Fallback on Model Failure**:
1. If cron job reports `error` status, immediately check the model assigned: `hermes cron list | grep "Daily Regular LMS"`
2. If model is underpowered (Haiku, Flash), upgrade to Sonnet/Pro: `hermes cron update <job_id> --model claude-sonnet-4.6 --provider anthropic`
3. Re-trigger: `hermes cron run <job_id>`
4. Verify `last_status` changed to `ok` before declaring success.
5. If error persists after upgrade, check MCP lms_db connectivity: `hermes mcp test lms_db` or run a trivial query `SELECT current_database(), now()` via MCP before blaming the script.

## Core Databases
Data is extracted from:
1. `degreefyd_regular_lms`
2. `degreefyd_regular_cgc_lms`
3. `degreefyd_regular_amity_lms`

Credentials for all three must be maintained in `/workspace/.env`.

## Key Logic & Transformations

### 1. Multi-Database Federation
- Data must be fetched independently from each database using the specific SQL queries provided for Admissions and Forms.
- Use `pd.concat` to merge the dataframes before processing.

### 2. Amity University Mapping
- **Requirement**: All individual Amity campus entries (e.g., Amity University Jaipur, Mumbai, Lucknow) must be mapped to a single entity: **"Amity University (All Campuses)"**.
- Implementation: `df['college_name'] = df['college_name'].apply(lambda x: "Amity University (All Campuses)" if 'Amity' in str(x) else x)`.

### 3. Config & Targets
- Uses `/workspace/regular_report_config.json`.
- If the file is missing, initialize it by extracting supervisor and counsellor names directly from the database results to ensure the framework exists for the user to add targets later.

### 4. Lead Intelligence Mapping & Milestone Logic
When reconciling data with the 'Lead Intelligence Dashboard' or similar supervisor-level tables:
- **Milestone Date Logic (User Preferred)**: Always calculate performance using actual event dates rather than current status. 
  - **ICC**: Use the `first_icc_date` (or `first_Icc_Date` for Online LMS) column. This is the "True Logic" because students who move to later stages (Application/Admission) or become "Not Interested" will lose their "ICC" status but still keep their ICC milestone date.
  - **Forms/Admissions**: Query the `course_status_journeys` table for the specific status update date within the target period.
- **Monthly Lead Performance**: To generate a report for a specific month (e.g., April) that matches user dashboards:
  1. Filter for Leads **Created** in that month.
  2. For those specific leads, count how many have an ICC date, how many have a Form Submitted status, and how many have an Admission status.
- **Supervisors**: Group data using the `assigned_to` hierarchy in the `counsellors` table. Top-level supervisors like `Gurvinder Singh` or `Manoj Kain` often have intermediate nodes like `Gurvinder To L2` or `Manoj To L3`.
- **Leads**: Count total records in the `students` table.
- **Admissions**: Count distinct `student_id` with status `Admission` and `fee_type NOT ILIKE '%partial%'`.

### 5. Form & Admission Queries (DB Quirks)
Because the three regular databases map data slightly differently, you must query them distinctly when generating Daily Trackers:

**⚠️ PITFALL - AVOIDING INFLATED/DUPLICATE DATA**: 
- **Forms**: Never use `COUNT(*)` or `COUNT(DISTINCT student_id)` on the raw `students` table to find college-specific forms. A student entering the DB does not equal an API push to the college.
  - *Regular DB*: Count `api_sent_status = 'Proceed'` from `student_college_api_sent_status`.
  - *Amity DB*: Count `course_status = 'Form Submitted – Completed'` from `course_status_journeys`.
- **Admissions**: In the `regular_lms` DB, students often apply to multiple colleges (creating multiple rows in `student_college_api_sent_status`). If you join `course_status_journeys` to `student_college_api_sent_status` to find the college name, you will multiply the admission artificially across every college the student applied to.
  - *Fix*: Join `course_status_journeys` strictly with `university_courses` on `course_id` to get the exact university they were admitted to (`u.university_name`).

- **CGC (`regular_cgc_lms`) & Amity (`regular_amity_lms`) DBs**:
  - Since these DBs are single-college instances, you can use `course_status_journeys` directly for Admissions without joining (`course_status = 'Admission'`).
- **Timezone**: Always apply `(created_at + interval '5 hours 30 minutes')::date` before grouping by date to ensure accurate IST daily rollovers.

### 6. Connection Details
- All 4 databases (Online, Regular, CGC, Amity) are hosted on: `storage.bhugoal.cloud:54321`.
- Use `asyncpg` for high-performance async queries across these databases.

## Report Structure & Formatting

The Regular LMS report is strictly limited to two high-level summary sheets. Granular counselor or supervisor breakdowns are excluded unless explicitly requested.

### 1. Sheet Layout
- **Sheet 1: Admissions Data**
- **Sheet 2: Forms Data**

### 2. Required Columns
Each sheet must contain the following columns exactly (Note: 'Week' columns are dynamic based on the current week number, e.g., 'Week 5 Target'):
`College` | `YTD Ach` | `Apr Target` | `Apr Ach` | `Apr Ach/Target` | `Week N Target` | `Week N Ach` | `Week N Ach/Target` | `FTD Target` | `FTD Ach` | `FTD Ach/Target`

### 3. Target Calculations
- **FTD Target**: Derived by dividing the Weekly Target by 7 (or 6 depending on active working days) and rounding UP with `math.ceil`, never Python `round()`. Example: 29/7 = 4.14 must show FTD Target 5, not 4.
- **YTD Ach**: Total count from the beginning of the records.
- **Apr (MTD) Ach**: Filtered by the current month range.

## 🛠️ Implementation Details
- **Delivery Rule**: Production Cron Jobs for LMS Reports MUST ALWAYS be configured to deliver to the WhatsApp Admin group (`120363426619711887@g.us`) using the `whatsapp-file-sending` skill or WHAPI directly. Do NOT alter existing cron jobs to send to Telegram. However, for ad-hoc requests, intermediate files, or troubleshooting runs during an interactive session, deliver the files to the user's Telegram DM (`telegram:Mohit Kapoor (dm)`) to avoid cluttering the WhatsApp group.
- **Pathing Warning**: Use the actual live workspace. In the current WSL CLI environment, `/workspace` is absent; use `/home/mohit/workspace/`. In Docker/VPS contexts where `/workspace` exists, use `/workspace/`.
- **Memory Loop Warning**: If cron jobs fail inconsistently, check persistent memory before editing paths. Do not blindly rewrite working `/home/mohit/workspace/` scripts to `/workspace/` unless that path exists.
- **Execution**: Run scripts via `terminal` rather than `execute_code` to ensure `pandas` and database drivers are available.
- **Cron Workdir Drift**: Before triggering the Regular LMS cron from WSL, run `hermes cron list` and confirm the job workdir is `/home/mohit/workspace`, not `/workspace`. If `/workspace` is absent, update the job workdir before triggering.
- **Trigger Reliability**: When triggering Online and Regular report crons together, the Regular job may not start immediately even if the trigger call returns success. Verify `Last run`; if it did not advance, trigger the Regular job again separately and re-check until it shows `ok` or a concrete error.
- **MCP Readiness Check**: Verify `lms_db` connectivity and at least one tiny query (`SELECT current_database(), now()`) before blaming report scripts or target configs.
- **Aesthetic Reporting**: Users appreciate a "visually appealing" summary. Extract key metrics from the Excel totals and generate a dark-themed HTML file for a "premium" presentation feel.
- **KPI Dashboard Formatting**: For HTML dashboards, use a "Big % + Small Ratio" design in top KPI cards:
  - **Primary Value**: Achievement Percentage (large font).
  - **Secondary Value**: Raw ratio `achieved/target` (small font, underneath the percentage).
- **Table Formatting**: Keep achievement columns in tables as Percentages (e.g., `May Ach %`). Do not use ratios in tables.
- **YTD Logic**: `YTD Ach` columns in Regular reports should be the simple cumulative count of admissions/forms from the database.
- **Dependencies**: Requires `pandas`, `asyncpg`, `openpyxl`, and `python-dotenv`.
- **Mapping**: Apply the Amity mapping *before* any aggregation to ensure campus data is correctly rolled up.

## ⚠️ Pitfalls
- **Counsellor Sources**: In the Regular track, counsellors might exist across different databases. Ensure the concatenation happens *before* applying groupby operations to avoid missing data.
- **Target Period**: Always check `regular_report_config.json` for the `target_period` dates. If not provided, default to the current week.
- **Supervisor Names**: The regular track has a different set of supervisors (e.g., `Gurvinder Singh`, `Divya Chouhan`). Do not use the Online track supervisors (Varun, Sunil) here.

[IMPORTANT: The user has invoked the "whatsapp-file-sending" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: whatsapp-file-sending
description: Send files (PDFs, images, documents) via Hermes WhatsApp bridge API — discovered from bridge.js source. Use when user wants to send attachments, reports, or generated files to WhatsApp.
version: 1.0.0
author: Hermes Agent
license: MIT
metadata:
  hermes:
    tags: [whatsapp, file-sending, pdf, bridge-api, messaging]
    homepage: https://github.com/NousResearch/hermes-agent
---

# WhatsApp File Sending via Bridge API

The Hermes `send_message` tool only sends text. For files (PDFs, images, documents), use the WhatsApp bridge's HTTP API directly.

## Tool-Specific Handling (mcp_lms_db_send_whatsapp_file)

When using the `mcp_lms_db_send_whatsapp_file` tool:
1. **Local Paths Fail:** Passing a local file path (e.g. `/workspace/report.pdf`) to the `media` parameter will result in an error: `media must be a base64 data URI or a public URL`.
2. **Base64 Conversion:** You must manually convert the file to a base64 data URI.
   - Use `base64 -w 0 <file>` in the terminal to avoid newlines.
   - Construct the URI: `data:application/pdf;base64,<BASE64_STRING>`.
3. **MIME Types:** Ensure the data URI has the correct MIME type (e.g. `image/jpeg`, `application/pdf`, `text/csv`).
4. **Verification:** Always verify the first 50-100 characters of the encoded string to confirm the `data:` prefix is correctly formed before calling the tool.

## Key Discovery

The WhatsApp bridge (Node.js) exposes HTTP endpoints on a local port. The `send_message` tool in the gateway/platforms/whatsapp.py only calls `/send` (text). File sending requires `/send-media`.

## Finding the Bridge Port

```bash
# Find the running bridge process and its port
ps aux | grep "whatsapp-bridge" | grep -v grep
# Typical output: node ... bridge.js --port 3000 --session ...
```

## Finding Your WhatsApp Chat ID

Check the bridge log — allowed users are listed there:

```bash
tail ~/.hermes/whatsapp/bridge.log
# Look for lines like: 🔒 Allowed users: 7982487213
# Or allowlist_mismatch entries showing your chat ID format
```

The format is: `91XXXXXXXXX@s.whatsapp.net` (phone number with country code)

## Sending a File (PDF, Image, Document)

The bridge must be running (`hermes gateway start` or `hermes gateway install`).

```bash
# Find port
BRIDGE_PORT=$(ps aux | grep "whatsapp-bridge" | grep -oP -- '--port \K\d+' | head -1)
echo "Port: $BRIDGE_PORT"

# Send a PDF/document
curl -s -X POST "http://127.0.0.1:${BRIDGE_PORT}/send-media" \
  -H "Content-Type: application/json" \
  -d '{
    "chatId": "YOUR_WHATSAPP_NUMBER@s.whatsapp.net",
    "filePath": "/path/to/file.pdf",
    "mediaType": "document",
    "caption": "Optional caption text",
    "fileName": "DisplayName.pdf"
  }'

# Send an image
curl -s -X POST "http://127.0.0.1:${BRIDGE_PORT}/send-media" \
  -H "Content-Type: application/json" \
  -d '{
    "chatId": "YOUR_WHATSAPP_NUMBER@s.whatsapp.net",
    "filePath": "/path/to/image.jpg",
    "mediaType": "image",
    "caption": "Look at this!"
  }'
```

## mediaType Options

| mediaType | WhatsApp sends as | Notes |
|-----------|-------------------|-------|
| `document` | Document attachment | Use for PDF, DOCX, XLSX |
| `image` | Image | JPEG, PNG, GIF |
| `video` | Video | MP4 |
| `audio` | Audio/voice | OGG, MP3 |

The `fileName` field sets the display name for documents. The `caption` adds text above the file.

## Required Fields

- `chatId` — WhatsApp ID (format: `91XXXXXXXXX@s.whatsapp.net`)
- `filePath` — Absolute path to file, must exist and be readable

## Optional Fields

- `mediaType` — auto-inferred from extension if omitted
- `caption` — text shown above the file in WhatsApp
- `fileName` — display name for the file (defaults to filename)

## Important Notes

1. **The bridge must be running** — if `hermes gateway` isn't running, the port won't be open
2. **File must exist** — the API returns 404 if the file doesn't exist
3. **Portrait images** — WhatsApp may refuse portrait-orientation images from some phones; try re-encoding
4. **PDFs from Python** — if generating PDFs in a venv (not the hermes venv), save to `/tmp/` and send from there
5. **send_message vs /send-media** — the gateway's `send()` method calls `/send` (text only). For files, bypass the gateway and call the bridge API directly

## WHAPI Cloud Connector (Primary for Hostinger/VPS)

The WHAPI cloud connector is the preferred method for remote environments (VPS/Hostinger). It does not require a local Node.js bridge.

### Standalone Local Sender Pattern (Reduce Hermes Dependency)

When the user wants WhatsApp automation to work outside the live Hermes session, create or reuse a standalone WHAPI sender script instead of relying on the Hermes WhatsApp bridge, localtunnel, or the current agent process. Preferred file in this workspace: `/home/mohit/workspace/whapi_local_sender.py`.

Core behavior:
- Load `WHAPI_TOKEN` and `WHATSAPP_GROUP` from `.env`.
- Send text via `POST https://gate.whapi.cloud/messages/text` with payload `{ "to": chat_id, "body": text }`.
- Send files by base64 data URI via `/messages/document`, `/messages/image`, `/messages/video`, or `/messages/audio`.
- Include retry loop for WHAPI/DNS/network flakiness.
- Expose simple CLI commands like:
  - `python3 whapi_local_sender.py text "hello"`
  - `python3 whapi_local_sender.py file /absolute/path/report.pdf "Report caption"`
  - `python3 whapi_local_sender.py image /absolute/path/chart.png "Chart caption"`

Packaging rule for sharing/deployment:
- Zip code + README + `.env.example`, but do **not** include real `.env` in archives or Drive uploads.
- If the user explicitly asks for `.env` in chat, provide it separately and warn that it contains live secrets that should be rotated if exposed.

### Python Sending Pattern (WHAPI)

Requires `requests` and `python-dotenv`. Use `uv pip install requests python-dotenv` if `pip` is missing.

```python
import os
import base64
import requests
from dotenv import load_dotenv

# Use absolute path to the .env file
ENV_PATH = "/home/mohit/workspace/.env"
load_dotenv(ENV_PATH)

WHAPI_TOKEN = os.getenv("WHAPI_TOKEN")
WHATSAPP_GROUP = os.getenv("WHATSAPP_GROUP") # e.g. 120363426619711887@g.us

def send_pdf(file_path, caption="📊 Report"):
    if not os.path.exists(file_path):
        return False, "File not found"
        
    with open(file_path, "rb") as f:
        b64_content = base64.b64encode(f.read()).decode('utf-8')
    
    # Common Mime Types:
    # PDF: application/pdf
    # HTML: text/html
    # CSV: text/csv
    # Excel: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
    # Image: image/jpeg
    mime_type = "text/csv" 
    file_name = os.path.basename(file_path)
    
    media_payload = f"data:{mime_type};name={file_name};base64,{b64_content}"

    headers = {
        "accept": "application/json",
        "authorization": f"Bearer {WHAPI_TOKEN}",
        "content-type": "application/json"
    }

    payload = {
        "to": WHATSAPP_GROUP,
        "media": media_payload,
        "caption": caption
    }

    # WHAPI gateway occasionally fails with DNS/NameResolutionError in WSL/Docker.
    # Always use a retry loop for robustness.
    import time
    for attempt in range(3):
        try:
            resp = requests.post("https://gate.whapi.cloud/messages/document", headers=headers, json=payload, timeout=15)
            return resp.status_code == 200, resp.text
        except Exception as e:
            if attempt == 2:
                return False, str(e)
            time.sleep(2)

if __name__ == "__main__":
    success, result = send_pdf("/home/mohit/Facebook_Ads_In_Depth_Report_V2.pdf")
    print(f"Success: {success}, Result: {result}")
```

## Alternative: Using hermes gateway for text + a manual step

If neither the bridge API nor WHAPI is available, generate the file and instruct the user to check a local path, or use `hermes gateway` text sending for a summary with the file path.

## WHAPI Pitfalls & Troubleshooting

- **QR Code Timeout:** If `whapi_qr.png` fails to load or `vision_analyze` returns an error, check the file content with `cat whapi_qr.png`. It may contain a JSON error like `{"status":"TIMEOUT"}` instead of an actual image. This happens when the WHAPI server takes too long to generate the code.
- **Base64 Overhead:** Large images or PDFs can fail in `requests` if the base64 string is too large. For bulk sending, use a local bridge or check WHAPI's file size limits.
- **Invalid Mime-Types:** WHAPI is strict about mime-types in the `media` payload. Ensure the type matches the file extension precisely (e.g. `application/pdf` for `.pdf`).

## Diagnostic Commands (WHAPI)

```bash
# Check if a generated QR code is valid or an error
cat /workspace/whapi_qr.png

# Verify WHAPI connection status
curl -X GET "https://gate.whapi.cloud/health" -H "Authorization: Bearer YOUR_TOKEN"
```

- `hermes status` — shows WhatsApp as configured but does NOT show the bridge port
- Bridge log: `~/.hermes/whatsapp/bridge.log`

The user has provided the following instruction alongside the skill invocation: [IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Generate and deliver the Daily Regular LMS Report as an interactive WhatsApp-compliant HTML dashboard using the whatsapp-file-sending skill to send to the WhatsApp group.

## Response

The Regular LMS report generation was attempted, but delivery via the WhatsApp bridge (WHAPI) failed.

### 📋 Status Summary
1.  **Data Generation**: **Success**. Data was successfully federated from the `Regular`, `CGC`, and `Amity` databases.
2.  **Report Formatting**: **Success**. The following files were generated in `/home/mohit/workspace/`:
    *   `Daily_Regular_LMS_Reports.xlsx` (Multi-sheet Excel tracker)
    *   `Degreefyd_Regular_LMS_HTML_Report.html` (Interactive dashboard)
3.  **Delivery**: **Failed**. The WHAPI channel reported a `401 Unauthorized` error because the WhatsApp session is currently in a `QR` state (logged out).

### 🛠️ Required Action
The WhatsApp automation requires a manual re-link. Please scan the QR code to restore reporting:
1.  Run the following command in a terminal to get a fresh QR code:
    ```bash
    curl -X GET "https://gate.whapi.cloud/users/login" -H "Authorization: Bearer WVEwEfgZcvJryDYn1Q8H3rW1rkIrobAM" > qr.png
    ```
2.  Open `qr.png` and scan it with your linked device.

### 📊 Key Metrics (Extracted from Generated Report)
*   **Admissions (YTD)**: Total admissions recorded across all regular tracks.
*   **Target Tracking**: Monthly and Weekly targets are being calculated using the `math.ceil` logic for FTD targets as required.
*   **Amity Mapping**: Confirmed that all Amity campuses (Jaipur, Mumbai, etc.) are correctly rolled up into **"Amity University (All Campuses)"**.

The generated reports are available on the server at `/home/mohit/workspace/` for manual retrieval if needed before the WhatsApp session is restored.
