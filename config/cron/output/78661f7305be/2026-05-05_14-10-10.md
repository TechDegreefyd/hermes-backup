# Cron Job: Daily Online LMS Reports

**Job ID:** 78661f7305be
**Run Time:** 2026-05-05 14:10:10
**Schedule:** 0 15 * * *

## Prompt

[IMPORTANT: The user has invoked the "online-lms-reporting" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: online-lms-reporting
description: How to process and generate the Daily Online LMS Reports from raw text/image targets provided by the user.
trigger: "When the user provides weekly targets via text or image and asks to update or send the Daily LMS Reports."
---

# Online LMS Reporting Workflow

The user prefers to drop raw text or images of targets into the chat and expects the agent to handle the configuration updates automatically. Do not ask them to edit `report_config.json` manually.

## Steps to Execute

1. **Extract Data**: Parse the provided text/images to identify the "Target Period" (e.g., 21st-30th April), the Supervisor targets, and the individual Counsellor targets.
2. **Update Config**: Write the extracted data to `/workspace/report_config.json`.
3. **Generate Reports**: Run `python3 /workspace/generate_daily_reports_v2.py`.
4. **Generate HTML Dashboard**: Convert the `Daily_Online_LMS_Reports_V2.xlsx` output into a WhatsApp-compliant interactive HTML file using CSS-only patterns. (See `whatsapp-html-reporting` skill for mandatory constraints: no JS, native `<details>` accordions, CSS radio-button tabs).
5. **Send Reports**: Run the WHAPI sending script to send the generated reports (HTML dashboard + Excel file) to WhatsApp.

## ⚠️ Critical Pitfalls & Past Mistakes

You MUST remember the following fixes to prevent past calculation errors:

1. **Naming Mismatches (Database vs. Config)**: 
   - **⚠️ String Accuracy**: Names must match exactly between the database and `report_config.json`. 
   - **Hidden Spaces**: Look for double spaces (e.g., `Om  Sharma` vs `Om Sharma`). 
   - **Spelling**: Names like `Siddarth Kumar` (DB) vs `Siddhartha` (Config) will cause data loss during the merge.
   - **Verification**: Always run a check for "unmatched records" when troubleshooting discrepancies.
2. **Time Window Separation & Midnight Buffer**: 
   - `FTD` (For The Day) is strictly *Today*. 
   - **⚠️ Midnight Buffer**: Since the server runs on UTC and reporting is for IST, reports triggered after midnight IST (but before 6:00 AM IST) should look back at the previous day. Implement a logic like: `if now_ist.hour < 6: report_date = now_ist - 1 day`.
   - `MTD` (Month To Date) is strictly the *1st of the current month to today* (e.g., 1st April to 30th April). Used primarily in the College Form/Admission table.
   - `Target Period/Week` is the specific custom date range provided by the user (e.g., 21st to 30th April). This is used for Revenue/Admission vs Target calculations. Do not mix these up.
3. **Pathing & Environment**: 
   - **⚠️ Absolute Paths**: Use the actual live workspace. In the current WSL CLI environment, `/workspace` is absent; use `/home/mohit/workspace/`. In Docker/VPS contexts where `/workspace` exists, use `/workspace/`.
   - **⚠️ Memory Loop Warning**: Check persistent memory before editing paths. Do not blindly rewrite working `/home/mohit/workspace/` scripts to `/workspace/` unless that path exists.
   - **Tool Limitation**: The `execute_code` sandbox may lack `pandas` or `asyncpg`. Always prefer running reporting scripts via the `terminal` tool with the Hermes venv Python (`/home/mohit/.hermes/hermes-agent/venv/bin/python`) to ensure dependencies and `.env` variables are loaded correctly.
- **Cron Workdir Drift**: Before triggering the Online LMS cron from WSL, run `hermes cron list` and confirm the job workdir is `/home/mohit/workspace`, not `/workspace`. If `/workspace` is absent, update the job workdir before triggering. Then verify `Last run` changed to `ok` after the run.
- **MCP Readiness Check**: If the report depends on database access, verify `lms_db` first with `hermes mcp test lms_db` or a tiny MCP `SELECT current_database(), now()` query before triggering the report cron.

## 🛠️ Recommended Enhancements
- **Aesthetic HTML Dashboard**: For higher engagement, extract the final metrics from the generated Excel and create a single-file dark-mode HTML dashboard to present key highlights visually. **CRITICAL:** Do NOT use Tailwind-like CDN CSS or JavaScript; strictly use inline/internal CSS to comply with WhatsApp's native document viewer limitations (refer to the `whatsapp-html-reporting` skill).
- **Cron Comparison**: Add a step in the cron job to run `extract_metrics.py`. This compares the current report against the previous day's report (`Daily_Online_LMS_Reports_Prev.xlsx`) to provide growth analysis.

[IMPORTANT: The user has invoked the "whatsapp-file-sending" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: whatsapp-file-sending
description: Send files (PDFs, images, documents) via Hermes WhatsApp bridge API — discovered from bridge.js source. Use when user wants to send attachments, reports, or generated files to WhatsApp.
version: 1.0.0
author: Hermes Agent
license: MIT
metadata:
  hermes:
    tags: [whatsapp, file-sending, pdf, bridge-api, messaging]
    homepage: https://github.com/NousResearch/hermes-agent
---

# WhatsApp File Sending via Bridge API

The Hermes `send_message` tool only sends text. For files (PDFs, images, documents), use the WhatsApp bridge's HTTP API directly.

## Key Discovery

The WhatsApp bridge (Node.js) exposes HTTP endpoints on a local port. The `send_message` tool in the gateway/platforms/whatsapp.py only calls `/send` (text). File sending requires `/send-media`.

## Finding the Bridge Port

```bash
# Find the running bridge process and its port
ps aux | grep "whatsapp-bridge" | grep -v grep
# Typical output: node ... bridge.js --port 3000 --session ...
```

## Finding Your WhatsApp Chat ID

Check the bridge log — allowed users are listed there:

```bash
tail ~/.hermes/whatsapp/bridge.log
# Look for lines like: 🔒 Allowed users: 7982487213
# Or allowlist_mismatch entries showing your chat ID format
```

The format is: `91XXXXXXXXX@s.whatsapp.net` (phone number with country code)

## Sending a File (PDF, Image, Document)

The bridge must be running (`hermes gateway start` or `hermes gateway install`).

```bash
# Find port
BRIDGE_PORT=$(ps aux | grep "whatsapp-bridge" | grep -oP -- '--port \K\d+' | head -1)
echo "Port: $BRIDGE_PORT"

# Send a PDF/document
curl -s -X POST "http://127.0.0.1:${BRIDGE_PORT}/send-media" \
  -H "Content-Type: application/json" \
  -d '{
    "chatId": "YOUR_WHATSAPP_NUMBER@s.whatsapp.net",
    "filePath": "/path/to/file.pdf",
    "mediaType": "document",
    "caption": "Optional caption text",
    "fileName": "DisplayName.pdf"
  }'

# Send an image
curl -s -X POST "http://127.0.0.1:${BRIDGE_PORT}/send-media" \
  -H "Content-Type: application/json" \
  -d '{
    "chatId": "YOUR_WHATSAPP_NUMBER@s.whatsapp.net",
    "filePath": "/path/to/image.jpg",
    "mediaType": "image",
    "caption": "Look at this!"
  }'
```

## mediaType Options

| mediaType | WhatsApp sends as | Notes |
|-----------|-------------------|-------|
| `document` | Document attachment | Use for PDF, DOCX, XLSX |
| `image` | Image | JPEG, PNG, GIF |
| `video` | Video | MP4 |
| `audio` | Audio/voice | OGG, MP3 |

The `fileName` field sets the display name for documents. The `caption` adds text above the file.

## Required Fields

- `chatId` — WhatsApp ID (format: `91XXXXXXXXX@s.whatsapp.net`)
- `filePath` — Absolute path to file, must exist and be readable

## Optional Fields

- `mediaType` — auto-inferred from extension if omitted
- `caption` — text shown above the file in WhatsApp
- `fileName` — display name for the file (defaults to filename)

## Important Notes

1. **The bridge must be running** — if `hermes gateway` isn't running, the port won't be open
2. **File must exist** — the API returns 404 if the file doesn't exist
3. **Portrait images** — WhatsApp may refuse portrait-orientation images from some phones; try re-encoding
4. **PDFs from Python** — if generating PDFs in a venv (not the hermes venv), save to `/tmp/` and send from there
5. **send_message vs /send-media** — the gateway's `send()` method calls `/send` (text only). For files, bypass the gateway and call the bridge API directly

## WHAPI Cloud Connector (Primary for Hostinger/VPS)

The WHAPI cloud connector is the preferred method for remote environments (VPS/Hostinger). It does not require a local Node.js bridge.

### Python Sending Pattern (WHAPI)

Requires `requests` and `python-dotenv`. Use `uv pip install requests python-dotenv` if `pip` is missing.

```python
import os
import base64
import requests
from dotenv import load_dotenv

# Use absolute path to the .env file
ENV_PATH = "/home/mohit/workspace/.env"
load_dotenv(ENV_PATH)

WHAPI_TOKEN = os.getenv("WHAPI_TOKEN")
WHATSAPP_GROUP = os.getenv("WHATSAPP_GROUP") # e.g. 120363426619711887@g.us

def send_pdf(file_path, caption="📊 Report"):
    if not os.path.exists(file_path):
        return False, "File not found"
        
    with open(file_path, "rb") as f:
        b64_content = base64.b64encode(f.read()).decode('utf-8')
    
    # Common Mime Types:
    # PDF: application/pdf
    # HTML: text/html
    # Excel: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
    # Image: image/jpeg
    mime_type = "application/pdf"
    file_name = os.path.basename(file_path)
    
    media_payload = f"data:{mime_type};name={file_name};base64,{b64_content}"

    headers = {
        "accept": "application/json",
        "authorization": f"Bearer {WHAPI_TOKEN}",
        "content-type": "application/json"
    }

    payload = {
        "to": WHATSAPP_GROUP,
        "media": media_payload,
        "caption": caption
    }

    # WHAPI gateway occasionally fails with DNS/NameResolutionError in WSL/Docker.
    # Always use a retry loop for robustness.
    import time
    for attempt in range(3):
        try:
            resp = requests.post("https://gate.whapi.cloud/messages/document", headers=headers, json=payload, timeout=15)
            return resp.status_code == 200, resp.text
        except Exception as e:
            if attempt == 2:
                return False, str(e)
            time.sleep(2)

if __name__ == "__main__":
    success, result = send_pdf("/home/mohit/Facebook_Ads_In_Depth_Report_V2.pdf")
    print(f"Success: {success}, Result: {result}")
```

## Alternative: Using hermes gateway for text + a manual step

If neither the bridge API nor WHAPI is available, generate the file and instruct the user to check a local path, or use `hermes gateway` text sending for a summary with the file path.

## WHAPI Pitfalls & Troubleshooting

- **QR Code Timeout:** If `whapi_qr.png` fails to load or `vision_analyze` returns an error, check the file content with `cat whapi_qr.png`. It may contain a JSON error like `{"status":"TIMEOUT"}` instead of an actual image. This happens when the WHAPI server takes too long to generate the code.
- **Base64 Overhead:** Large images or PDFs can fail in `requests` if the base64 string is too large. For bulk sending, use a local bridge or check WHAPI's file size limits.
- **Invalid Mime-Types:** WHAPI is strict about mime-types in the `media` payload. Ensure the type matches the file extension precisely (e.g. `application/pdf` for `.pdf`).

## Diagnostic Commands (WHAPI)

```bash
# Check if a generated QR code is valid or an error
cat /workspace/whapi_qr.png

# Verify WHAPI connection status
curl -X GET "https://gate.whapi.cloud/health" -H "Authorization: Bearer YOUR_TOKEN"
```

- `hermes status` — shows WhatsApp as configured but does NOT show the bridge port
- Bridge log: `~/.hermes/whatsapp/bridge.log`

The user has provided the following instruction alongside the skill invocation: [IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Generate and deliver the Daily Online LMS Report as an interactive WhatsApp-compliant HTML dashboard.

Working directory: /workspace.

Required steps:
1. Run `python3 /workspace/generate_daily_reports_v2.py` to refresh `/workspace/Daily_Online_LMS_Reports_V2.xlsx` using `/workspace/report_config.json`.
2. Run `python3 /workspace/generate_and_send_lms_html.py` to generate `/workspace/Degreefyd_Online_LMS_HTML_Report.html` and send it to WhatsApp Admin group via WHAPI.
3. Do NOT send the Excel file unless explicitly requested. The primary delivery is HTML.
4. If DB/WHAPI has a transient timeout, retry the failed command up to 3 times with a short delay.
5. Final response must state whether the HTML file was generated and whether WHAPI delivery succeeded.

Current target rules already saved in `/workspace/report_config.json`:
- May 2026 target period: 2026-05-01 to 2026-05-31.
- Team owner admission targets: Sunil 80, Varun 80, Siddhartha/Siddarth Kumar 80, Vishal/Vishal Gaur 40; total 280.
- Team owner fee targets: Sunil ₹50,00,000, Varun ₹50,00,000, Siddhartha/Siddarth Kumar ₹50,00,000, Vishal/Vishal Gaur ₹2,00,000; total ₹1,52,00,000.
- Counsellor fee targets are intentionally zero; only team owners have targets.

## Response

The Daily Online LMS Report for **May 5, 2026**, has been successfully processed and delivered.

### **Execution Summary**
1. **Excel Report Generation**: `generate_daily_reports_v2.py` executed successfully. 
   - **Target Period**: 2026-05-01 to 2026-05-31 (May 2026).
   - **File Updated**: `/home/mohit/workspace/Daily_Online_LMS_Reports_V2.xlsx`.
2. **HTML Dashboard Generation**: `generate_and_send_lms_html.py` executed successfully.
   - **File Generated**: `/home/mohit/workspace/Degreefyd_Online_LMS_HTML_Report.html`.
3. **WhatsApp Delivery**: **Succeeded** via WHAPI.
   - **Destination**: WhatsApp Admin Group (`120363426619711887@g.us`).
   - **Status**: 200 OK (Message ID: `PspsFj9LjAtbzeA-wlIBq53mF53hjw`).

The report includes FTD (For The Day), MTD (Month To Date), and Target Period metrics for Sunil, Varun, Siddarth Kumar, and Vishal Gaur based on the current configuration.
