# Cron Job: Daily Online LMS Reports

**Job ID:** 78661f7305be
**Run Time:** 2026-05-11 16:22:15
**Schedule:** 0 15 * * *

## Prompt

[IMPORTANT: The user has invoked the "online-lms-reporting" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: online-lms-reporting
description: How to process and generate the Daily Online LMS Reports from raw text/image targets provided by the user.
trigger: "When the user provides weekly targets via text or image and asks to update or send the Daily LMS Reports."
---

# Online LMS Reporting Workflow

The user prefers to drop raw text or images of targets into the chat and expects the agent to handle the configuration updates automatically. Do not ask them to edit `report_config.json` manually.

References:
- `references/online-lms-data-gap-audit.md` — Audit playbook and known production misses (Vishwajeet trailing-space, Prerna/Prena typo) for missing admission/data-gap complaints.
- `references/model-failure-recovery.md` — Diagnostic flow and fallback steps when cron jobs fail due to model capacity or connectivity issues.

## Model Requirements & Fallback

⚠️ **CRITICAL**: This skill requires a model capable of:
- Multi-step orchestration (config parsing → database queries → file generation → delivery)
- Complex data transformations (pandas, SQL joins, timezone arithmetic)
- HTML/CSS generation for WhatsApp compliance

**Minimum Model Tier**: Claude Sonnet 3.5+ or Gemini 3.1 Pro.
**DO NOT use**: Haiku, Flash Lite, or any "fast" tier — they will fail silently or produce malformed output.

**Fallback on Model Failure**:
1. If cron job reports `error` status, immediately check the model assigned: `hermes cron list | grep "Daily Online LMS"`
2. If model is underpowered (Haiku, Flash), upgrade to Sonnet/Pro: `hermes cron update <job_id> --model claude-sonnet-4.6 --provider anthropic`
3. Re-trigger: `hermes cron run <job_id>`
4. Verify `last_status` changed to `ok` before declaring success.

## Steps to Execute

1. **Extract Data**: Parse the provided text/images to identify the "Target Period" (e.g., 21st-30th April), the Supervisor targets, and the individual Counsellor targets.
2. **Update Config**: Write the extracted data to `/workspace/report_config.json` (or `/home/mohit/workspace/report_config.json` in WSL/local contexts).
3. **Generate Reports**: Run `python3 /workspace/generate_daily_reports_v2.py`.
4. **Generate HTML Dashboard**: Convert the `Daily_Online_LMS_Reports_V2.xlsx` output into a WhatsApp-compliant interactive HTML file using CSS-only patterns. (See `whatsapp-html-reporting` skill for mandatory constraints: no JS, native `<details>` accordions, CSS radio-button tabs).
5. **Send Reports**: Production Cron Jobs for LMS Reports MUST ALWAYS be configured to deliver to the WhatsApp Admin group (`120363426619711887@g.us`) using the `whatsapp-file-sending` skill or WHAPI directly. Do NOT alter existing cron jobs to send to Telegram. However, for ad-hoc requests, intermediate files, or troubleshooting runs during an interactive session, deliver the files to the user's Telegram DM (`telegram:Mohit Kapoor (dm)`) using the `send_message` tool. DO NOT send these ad-hoc reports to WhatsApp unless explicitly requested.

## ⚠️ Critical Pitfalls — DO NOT REPEAT

### 1. Vishal Gaur Fee Target = ₹20 LAKH (2000000)
`report_config.json` → `supervisor_targets.Vishal Gaur` MUST be `2000000` (₹20,00,000), NEVER `200000` (₹2 lakh). This has been repeatedly mistaken. Verify:
- Config before every Excel generation: `grep -A1 Vishal report_config.json`
- HTML output before sending: grep for `Vishal.*rev">₹` — first match should be ₹2,000,000

### 2. WHAPI Hang & Silent HTML Failure
The HTML generation scripts call WHAPI which can hang. Do NOT use `content.replace('requests.post', '# requests.post')` — creates `r = # ...` syntax error.
**Correct bypass**: `content.replace("r = requests.post", "r = None  # skip")` and `content.replace("if r and r.status_code", "if False  # skip")`.
Always delete old HTML before running scripts: `rm -f Degreefyd_Online_LMS_HTML_Report.html`
- HTML output after generation: `grep "Vishal.*rev" output.html` — must show `₹2,000,000` or `₹20L`, NOT `₹200,000`
- Never trust cached HTML files — always regenerate fresh

### 2. WHAPI Script Hang — Silent Failure
`generate_and_send_lms_html.py` and `generate_and_send_regular_lms_html.py` hang indefinitely on WHAPI network calls. When run via subprocess, they timeout silently without generating output. The old cached HTML file remains.
- Always delete the old HTML output file BEFORE running the script
- Verify the new file was created after the script runs
- If script hangs, generate HTML directly from Excel using standalone Python (read Excel with pandas, build HTML string, write to file)
- NEVER trust a file that wasn't freshly created by a successful run

### 3. Config Key Names
- Fee targets: `supervisor_targets` (NOT `supervisor_fee_targets`)
- Admission targets: `supervisor_admission_targets`
- Regular config: `regular_report_config.json` (path bug: script uses `'workspace/regular_report_config.json'` which resolves relative to CWD — fix to `'regular_report_config.json'`)

### 4. Naming Mismatches (Database vs. Config): 
    - Lead attribution to Meta campaigns is often stored in the `first_source_url` column of the `students` table via UTM parameters. Use this for mapping if `meta_ads_leads` is empty.
    - Verified lead counts (Marketing counts) include all new student registrations for the day, not just those with `course_status = 'Admission'`.
    - Use IST offset `+05:30` for `created_at` queries.
    - 2. **...**
   - **Hidden Spaces / Trailing Spaces**: Normalize all supervisor/counsellor names before merging: trim leading/trailing whitespace and collapse repeated whitespace. Real production miss: DB had `Vishwajeet ` (trailing space) while config had `Vishwajeet`, causing his admission/revenue to disappear from counsellor/supervisor tables even though college totals were correct.
   - **Double Spaces**: Look for double spaces (e.g., `Om  Sharma` vs `Om Sharma`).
   - **Spelling**: Names like `Siddarth Kumar` (DB) vs `Siddhartha` (Config), or `Prerna Jain` (DB) vs `Prena Jain` (Config), will cause data loss during the merge.
   - **Verification**: Always run a check for "unmatched records" when troubleshooting discrepancies. Compare raw DB MTD/FTD admission counts against generated Excel totals and list any `(supervisor_name, counsellor_name)` pairs not present in config.
2. **Time Window Separation & Midnight Buffer**: 
   - `FTD` (For The Day) is strictly *Today*. 
   - **⚠️ Midnight Buffer**: Since the server runs on UTC and reporting is for IST, reports triggered after midnight IST (but before 6:00 AM IST) should look back at the previous day. Implement a logic like: `if now_ist.hour < 6: report_date = now_ist - 1 day`.
   - `MTD` (Month To Date) is strictly the *1st of the current month to today* (e.g., 1st April to 30th April). Used primarily in the College Form/Admission table.
   - `Target Period/Week` is the specific custom date range provided by the user (e.g., 21st to 30th April). This is used for Revenue/Admission vs Target calculations. Do not mix these up.
3. **Pathing & Environment**: 
   - **⚠️ Absolute Paths**: Use the actual live workspace. In the current WSL CLI environment, `/workspace` is absent; use `/home/mohit/workspace/`. In Docker/VPS contexts where `/workspace` exists, use `/workspace/`.
   - **⚠️ Memory Loop Warning**: Check persistent memory before editing paths. Do not blindly rewrite working `/home/mohit/workspace/` scripts to `/workspace/` unless that path exists.
   - **Tool Limitation**: The `execute_code` sandbox may lack `pandas` or `asyncpg`. Always prefer running reporting scripts via the `terminal` tool with the Hermes venv Python (`/home/mohit/.hermes/hermes-agent/venv/bin/python`) to ensure dependencies and `.env` variables are loaded correctly.
- **Cron Workdir Drift**: Before triggering the Online LMS cron from WSL, run `hermes cron list` and confirm the job workdir is `/home/mohit/workspace`, not `/workspace`. If `/workspace` is absent, update the job workdir before triggering. Then verify `Last run` changed to `ok` after the run.
- **MCP Readiness Check**: If the report depends on database access, verify `lms_db` first with `hermes mcp test lms_db` or a tiny MCP `SELECT current_database(), now()` query before triggering the report cron.

## 🧪 Debugging / Data-Gap Audit Procedure
When the user reports a missing admission or college-wise gap, do not guess from the rendered HTML alone. Use this sequence:
1. Query raw `online_lms` via MCP/DB for the named counsellor/student and target window, including `student_id`, `student_name`, `college_name`, `fee_type`, `deposit_amount`, `created_at AT TIME ZONE 'Asia/Kolkata'`, current counsellor, journey counsellor, enrollment counsellor, and supervisor.
2. Run raw college-wise MTD/FTD admissions with the same business rule used in reports: `course_status = 'Admission'` and `fee_type NOT ILIKE '%partial%'`.
3. Read `Daily_Online_LMS_Reports_V2.xlsx` sheets (`Counsellor_Admission`, `Counsellor_Fee_Collected`, `Supervisor_Admission`, `Supervisor_Fee_Collected`, `College_Performance`) and compare totals.
4. Run an unmatched-record audit: normalize DB and config names, then list raw admissions whose `(supervisor_name, counsellor_name)` pair is missing from `report_config.json`.
5. Patch the root cause only after identifying whether the gap is (a) whitespace/name mismatch, (b) config spelling typo, (c) partial-fee exclusion, (d) date window/timezone issue, or (e) query join field issue.
6. Regenerate the Excel locally, verify the named counsellor row and grand totals, then trigger the Online LMS cron and verify `Last run ... ok`.

## 🛠️ Recommended Enhancements
- **Aesthetic HTML Dashboard**: For higher engagement, extract the final metrics from the generated Excel and create a single-file dark-mode HTML dashboard to present key highlights visually. **CRITICAL:** Do NOT use Tailwind-like CDN CSS or JavaScript; strictly use inline/internal CSS to comply with WhatsApp's native document viewer limitations (refer to the `whatsapp-html-reporting` skill).
- **User-facing wording**: Strictly label online fee/deposit metrics as "Fee Collected" (never "Revenue") in all Excel sheets, headers, HTML dashboards, and message captions.
- **KPI Dashboard Formatting**: The user prefers a specific "Big % + Small Ratio" design for KPI boxes and Team Owner snapshot cards:
  - **Primary Value**: Achievement Percentage (e.g., `12.5%`) in large, prominent font.
  - **Secondary Value**: The raw ratio `achieved/target` (e.g., `5.7L/50L` or `39,000/20L`) in smaller text directly underneath the percentage.
- **Table Formatting**: Achievement columns within data tables should remain as Percentages (e.g., `Ach %`, `F2A %`). Do not replace table percentages with ratios.
- **Cron Comparison**: Add a step in the cron job to run `extract_metrics.py`. This compares the current report against the previous day's report (`Daily_Online_LMS_Reports_Prev.xlsx`) to provide growth analysis.

[IMPORTANT: The user has invoked the "whatsapp-file-sending" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: whatsapp-file-sending
description: Send files (PDFs, images, documents) via Hermes WhatsApp bridge API — discovered from bridge.js source. Use when user wants to send attachments, reports, or generated files to WhatsApp.
version: 1.0.0
author: Hermes Agent
license: MIT
metadata:
  hermes:
    tags: [whatsapp, file-sending, pdf, bridge-api, messaging]
    homepage: https://github.com/NousResearch/hermes-agent
---

# WhatsApp File Sending via Bridge API

The Hermes `send_message` tool only sends text. For files (PDFs, images, documents), use the WhatsApp bridge's HTTP API directly.

## Tool-Specific Handling (mcp_lms_db_send_whatsapp_file)

When using the `mcp_lms_db_send_whatsapp_file` tool:
1. **Local Paths Fail:** Passing a local file path (e.g. `/workspace/report.pdf`) to the `media` parameter will result in an error: `media must be a base64 data URI or a public URL`.
2. **Base64 Conversion:** You must manually convert the file to a base64 data URI.
   - Use `base64 -w 0 <file>` in the terminal to avoid newlines.
   - Construct the URI: `data:application/pdf;base64,<BASE64_STRING>`.
3. **MIME Types:** Ensure the data URI has the correct MIME type (e.g. `image/jpeg`, `application/pdf`, `text/csv`).
4. **Verification:** Always verify the first 50-100 characters of the encoded string to confirm the `data:` prefix is correctly formed before calling the tool.

## Key Discovery

The WhatsApp bridge (Node.js) exposes HTTP endpoints on a local port. The `send_message` tool in the gateway/platforms/whatsapp.py only calls `/send` (text). File sending requires `/send-media`.

## Finding the Bridge Port

```bash
# Find the running bridge process and its port
ps aux | grep "whatsapp-bridge" | grep -v grep
# Typical output: node ... bridge.js --port 3000 --session ...
```

## Finding Your WhatsApp Chat ID

Check the bridge log — allowed users are listed there:

```bash
tail ~/.hermes/whatsapp/bridge.log
# Look for lines like: 🔒 Allowed users: 7982487213
# Or allowlist_mismatch entries showing your chat ID format
```

The format is: `91XXXXXXXXX@s.whatsapp.net` (phone number with country code)

## Sending a File (PDF, Image, Document)

The bridge must be running (`hermes gateway start` or `hermes gateway install`).

```bash
# Find port
BRIDGE_PORT=$(ps aux | grep "whatsapp-bridge" | grep -oP -- '--port \K\d+' | head -1)
echo "Port: $BRIDGE_PORT"

# Send a PDF/document
curl -s -X POST "http://127.0.0.1:${BRIDGE_PORT}/send-media" \
  -H "Content-Type: application/json" \
  -d '{
    "chatId": "YOUR_WHATSAPP_NUMBER@s.whatsapp.net",
    "filePath": "/path/to/file.pdf",
    "mediaType": "document",
    "caption": "Optional caption text",
    "fileName": "DisplayName.pdf"
  }'

# Send an image
curl -s -X POST "http://127.0.0.1:${BRIDGE_PORT}/send-media" \
  -H "Content-Type: application/json" \
  -d '{
    "chatId": "YOUR_WHATSAPP_NUMBER@s.whatsapp.net",
    "filePath": "/path/to/image.jpg",
    "mediaType": "image",
    "caption": "Look at this!"
  }'
```

## mediaType Options

| mediaType | WhatsApp sends as | Notes |
|-----------|-------------------|-------|
| `document` | Document attachment | Use for PDF, DOCX, XLSX |
| `image` | Image | JPEG, PNG, GIF |
| `video` | Video | MP4 |
| `audio` | Audio/voice | OGG, MP3 |

The `fileName` field sets the display name for documents. The `caption` adds text above the file.

## Required Fields

- `chatId` — WhatsApp ID (format: `91XXXXXXXXX@s.whatsapp.net`)
- `filePath` — Absolute path to file, must exist and be readable

## Optional Fields

- `mediaType` — auto-inferred from extension if omitted
- `caption` — text shown above the file in WhatsApp
- `fileName` — display name for the file (defaults to filename)

## Important Notes

1. **The bridge must be running** — if `hermes gateway` isn't running, the port won't be open
2. **File must exist** — the API returns 404 if the file doesn't exist
3. **Portrait images** — WhatsApp may refuse portrait-orientation images from some phones; try re-encoding
4. **PDFs from Python** — if generating PDFs in a venv (not the hermes venv), save to `/tmp/` and send from there
5. **send_message vs /send-media** — the gateway's `send()` method calls `/send` (text only). For files, bypass the gateway and call the bridge API directly

## WHAPI Cloud Connector (Primary for Hostinger/VPS)

The WHAPI cloud connector is the preferred method for remote environments (VPS/Hostinger). It does not require a local Node.js bridge.

### Standalone Local Sender Pattern (Reduce Hermes Dependency)

When the user wants WhatsApp automation to work outside the live Hermes session, create or reuse a standalone WHAPI sender script instead of relying on the Hermes WhatsApp bridge, localtunnel, or the current agent process. Preferred file in this workspace: `/home/mohit/workspace/whapi_local_sender.py`.

Core behavior:
- Load `WHAPI_TOKEN` and `WHATSAPP_GROUP` from `.env`.
- Send text via `POST https://gate.whapi.cloud/messages/text` with payload `{ "to": chat_id, "body": text }`.
- Send files by base64 data URI via `/messages/document`, `/messages/image`, `/messages/video`, or `/messages/audio`.
- Include retry loop for WHAPI/DNS/network flakiness.
- Expose simple CLI commands like:
  - `python3 whapi_local_sender.py text "hello"`
  - `python3 whapi_local_sender.py file /absolute/path/report.pdf "Report caption"`
  - `python3 whapi_local_sender.py image /absolute/path/chart.png "Chart caption"`

Packaging rule for sharing/deployment:
- Zip code + README + `.env.example`, but do **not** include real `.env` in archives or Drive uploads.
- If the user explicitly asks for `.env` in chat, provide it separately and warn that it contains live secrets that should be rotated if exposed.

### Python Sending Pattern (WHAPI)

Requires `requests` and `python-dotenv`. Use `uv pip install requests python-dotenv` if `pip` is missing.

```python
import os
import base64
import requests
from dotenv import load_dotenv

# Use absolute path to the .env file
ENV_PATH = "/home/mohit/workspace/.env"
load_dotenv(ENV_PATH)

WHAPI_TOKEN = os.getenv("WHAPI_TOKEN")
WHATSAPP_GROUP = os.getenv("WHATSAPP_GROUP") # e.g. 120363426619711887@g.us

def send_pdf(file_path, caption="📊 Report"):
    if not os.path.exists(file_path):
        return False, "File not found"
        
    with open(file_path, "rb") as f:
        b64_content = base64.b64encode(f.read()).decode('utf-8')
    
    # Common Mime Types:
    # PDF: application/pdf
    # HTML: text/html
    # CSV: text/csv
    # Excel: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
    # Image: image/jpeg
    mime_type = "text/csv" 
    file_name = os.path.basename(file_path)
    
    media_payload = f"data:{mime_type};name={file_name};base64,{b64_content}"

    headers = {
        "accept": "application/json",
        "authorization": f"Bearer {WHAPI_TOKEN}",
        "content-type": "application/json"
    }

    payload = {
        "to": WHATSAPP_GROUP,
        "media": media_payload,
        "caption": caption
    }

    # WHAPI gateway occasionally fails with DNS/NameResolutionError in WSL/Docker.
    # Always use a retry loop for robustness.
    import time
    for attempt in range(3):
        try:
            resp = requests.post("https://gate.whapi.cloud/messages/document", headers=headers, json=payload, timeout=15)
            return resp.status_code == 200, resp.text
        except Exception as e:
            if attempt == 2:
                return False, str(e)
            time.sleep(2)

if __name__ == "__main__":
    success, result = send_pdf("/home/mohit/Facebook_Ads_In_Depth_Report_V2.pdf")
    print(f"Success: {success}, Result: {result}")
```

## Alternative: Using hermes gateway for text + a manual step

If neither the bridge API nor WHAPI is available, generate the file and instruct the user to check a local path, or use `hermes gateway` text sending for a summary with the file path.

## WHAPI Pitfalls & Troubleshooting

- **QR Code Timeout:** If `whapi_qr.png` fails to load or `vision_analyze` returns an error, check the file content with `cat whapi_qr.png`. It may contain a JSON error like `{"status":"TIMEOUT"}` instead of an actual image. This happens when the WHAPI server takes too long to generate the code.
- **Base64 Overhead:** Large images or PDFs can fail in `requests` if the base64 string is too large. For bulk sending, use a local bridge or check WHAPI's file size limits.
- **Invalid Mime-Types:** WHAPI is strict about mime-types in the `media` payload. Ensure the type matches the file extension precisely (e.g. `application/pdf` for `.pdf`).

## Diagnostic Commands (WHAPI)

```bash
# Check if a generated QR code is valid or an error
cat /workspace/whapi_qr.png

# Verify WHAPI connection status
curl -X GET "https://gate.whapi.cloud/health" -H "Authorization: Bearer YOUR_TOKEN"
```

- `hermes status` — shows WhatsApp as configured but does NOT show the bridge port
- Bridge log: `~/.hermes/whatsapp/bridge.log`

The user has provided the following instruction alongside the skill invocation: [IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Generate and deliver the Daily Online LMS Report.

⚠️ CRITICAL: Vishal Gaur fee target = ₹20,00,000 (2000000) in /home/mohit/workspace/report_config.json under supervisor_targets. NEVER change this to 2 lakh.

Steps:
1. Run: /home/mohit/.hermes/hermes-agent/venv/bin/python /home/mohit/workspace/generate_daily_reports_v2.py
2. Run: /home/mohit/.hermes/hermes-agent/venv/bin/python /home/mohit/workspace/generate_and_send_lms_html.py
3. HTML auto-sends via WHAPI. If WHAPI fails, save as Online_LMS_HTML_{date}.html and send to Telegram DM.

Workdir: /home/mohit/workspace

## Response

The Daily Online LMS Report for **May 11, 2026**, has been generated.

### 📊 Report Summary
- **FTD (For The Day):** 2026-05-11
- **MTD (Month To Date):** 2026-05-01 to 2026-05-11
- **Target Period:** 2026-05-01 to 2026-05-31
- **Vishal Gaur Target Verification:** Confirmed at **₹2,000,000** (2.0% achievement tracked in HTML).

### 🛠️ Execution Details
1. **Excel Generation:** Successfully created `Daily_Online_LMS_Reports_V2.xlsx`.
2. **HTML Dashboard:** Successfully created `Degreefyd_Online_LMS_HTML_Report.html`.
3. **Delivery Status:** 
   - **WHAPI:** Failed with `Channel not found` (likely a configuration or session issue on the WHAPI gateway).
   - **Fallback:** As per instructions, the HTML report has been saved locally as `Online_LMS_HTML_2026-05-11.html`.

### 📂 Files Generated
- `/home/mohit/workspace/Daily_Online_LMS_Reports_V2.xlsx`
- `/home/mohit/workspace/Online_LMS_HTML_2026-05-11.html`

The system will now deliver this final status to the configured destination.
